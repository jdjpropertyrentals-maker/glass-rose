Terraform infra (placeholder)

This folder will contain Terraform configurations for cloud infrastructure, VPCs, managed databases, and geoblocking/CDN rules.

Notes:
- Provide variables for cloud region, project ID, and allowed geofences.
- Keep sensitive provider credentials in a separate secrets store (Vault / cloud KMS).
